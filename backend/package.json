{
  "name": "pollen-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
